# Firebase Auth Task

This project implements Google login using Firebase Authentication.

- Files included:
  - `index.js` → login page
  - `firebase.js` → Firebase config
  - `auth.js` → Google login function

Open `index.html` in your browser and click the "Login with Google" button to test.
